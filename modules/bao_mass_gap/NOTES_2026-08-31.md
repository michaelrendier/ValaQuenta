# NOTES 2026-08-31 — engine reframe (no code change)

The maths in `maths.py` is unchanged. This note records the conceptual frame
the equations now sit in; see `ValaQuenta/wiki/bao_mass_gap.md` §"The gap is
the first motion" for the full text.

- `Δ = Ω_ζΣ − D*·ln10` is **the first motion of recombination** — the ignition
  impulse of the hydrogen engine (`FourthAgePapers/RecombinationMisfires`).
  Below `Δ`: no motion (the `d*` boundary). At `Δ`: the first stroke.
- `Δ` is the **quantum of Perfect Perturbation** — the smallest `Re σ_RB`
  (tilt / Poynting) the field can carry. `project_oblique_gear`: the Oblique
  Gear is Maxwell.
- **One `Δ`, every rung** — QGP (`t<1µs`) through recombination (`z≈1100`) to
  `d*`. Scale invariance = `d*_RG`'s dimensional fixed point (`DStarRG`).
- Timing is built in: **4 (d* faces) : 3 (Lambert W faces)**, `lcm = 12`, no
  camshaft — the engine times itself by its shape.

Status unchanged: `Δ > 0` ESTABLISHED; the closed form `1/(1000√2)` and
"measured as the residue exactly" remain THEORETICAL (the `10³` is undthe
d*_RG doubling count, not derived).
