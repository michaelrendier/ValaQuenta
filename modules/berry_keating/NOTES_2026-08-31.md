# NOTES 2026-08-31 — engine reframe (no code change)

`maths.py` unchanged. Frame the equations now sit in; full text in
`ValaQuenta/wiki/berry_keating.md` §"The clock is built in".

- `H = xp` generates `x → eᵗx`. **The flow IS the clock** — no external timing,
  like a Wankel has no camshaft. Timing structure = **4 (d* faces) : 3
  (Lambert W / Ω_ZS rotor faces)**, `lcm = 12` → the orbit precesses (flowers)
  rather than closing.
- EM-side reading: `σ_RB` is the hypercomplex Riemann–Silberstein vector, so
  the RedBlue field is Maxwell. `σ = ½` = amplitude-perfect functional-equation
  reflection = a pure standing wave whose nodes are the zeros (the Cymatic
  Nodal Line, RHP §6.3.1). The **mass gap `Δ`** = the width of the first band
  above that nodal line = the ignition impulse of recombination.
- Third bearing on `Re(s)=½` (Courant `Y₁⁰`, Noether current, standing wave).
  None closes C1. Confidence floor stays **OPEN**.
